window.addressapp = angular.module('addressApp', ['ngResource']);

// injecting resource code into application
// resource allows you to make API calls to the webpage

// var app = angular.module('').....
/*
app.config(function () {

});

app.run(function ($http) {

  var resp = $http.post('asdf', {});
  resp.then(function (re) {
    re.data
    re.headers
    re.status
  });
});*/